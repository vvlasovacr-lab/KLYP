# Reels Studio — техническая документация пайплайна

> **Осторожно, документ частично устарел.**
>
> Он описывает первую версию монтажа — ту, что жила в `server/pipeline/`
> (`transcribe.js` → `plan.js` → `render.js`) и `src/*.jsx`. С тех пор
> монтаж переехал в `engine/`: отдельный движок на Python, который умеет
> речевой монтаж, трекинг лица, вёрстку по метрикам шрифта и контроль
> качества. Воркер зовёт его через [engine.js](../server/pipeline/engine.js).
>
> Актуальны здесь разделы про **очередь, хранение, платежи, авторизацию
> и доставку** — эта половина системы не менялась. Разделы про разметку
> и рендер описывают старый путь; он остался в репозитории, но в работе
> не участвует. Про новый — см. README, раздел «Разделение труда».

Разбор реализованной системы автоматического видеомонтажа.

Документ описывает **то, что реально в коде** на момент написания.
Нереализованные вещи отмечены явно — на них нельзя закладываться при переносе.

---

## 1. Общая архитектура

### 1.1 Стек

| Слой | Технология | Почему именно она |
|---|---|---|
| Рендер видео | **Remotion 4.x** (React) | Кадр описывается как React-компонент. Анимация — чистая функция от номера кадра, значит результат детерминирован и воспроизводим. Вёрстка текста, градиенты, тени, блюр — обычный CSS, который не надо реализовывать вручную как во фреймсерверных решениях |
| Обработка медиа | **ffmpeg / ffprobe** | Извлечение аудио, детект тишины, длительность, генерация обложки |
| Бэкенд | **Node.js 20+ (ESM), Fastify** | Один язык с Remotion — общий код и типы данных между рендером и API. Fastify выбран за низкий оверхед и встроенную поддержку multipart |
| Очередь | **PostgreSQL** (`SELECT … FOR UPDATE SKIP LOCKED`) | Задач мало, но они очень долгие (минуты). Redis добавил бы вторую инфраструктурную зависимость ради того, что таблица делает надёжнее — с транзакциями и без потери задач при рестарте |
| Бот | **grammY** | Современный API, нативный ESM, типизирован |
| Хранилище | Файловая система (том) | Видео большие; объектное хранилище усложняет локальную разработку. Слой изолирован — заменяется на S3 в одном модуле |

### 1.2 Ключевое архитектурное решение

**Пайплайн разбит на три независимых шага, связанных JSON-контрактом.**

```
                     ┌──────────────────────────────────────┐
input video  ──────► │ 1. ОБРАБОТКА (transcribe.js)         │
  (mp4/mov)          │    ffmpeg: длительность + детект     │
                     │    тишины → границы реплик           │
                     └──────────────┬───────────────────────┘
                                    │  chunks[]
                                    ▼
                     ┌──────────────────────────────────────┐
                     │ 2. АНАЛИЗ + МОНТАЖНЫЙ ПЛАН (plan.js) │
                     │    шаблон или LLM → что подсветить,  │
                     │    где врезка, какая плашка          │
                     └──────────────┬───────────────────────┘
                                    │  plan{}
                                    ▼
                     ┌──────────────────────────────────────┐
                     │ 3. РЕНДЕР (render.js → Remotion CLI) │
                     │    React-компоненты + props → кадры  │
                     └──────────────┬───────────────────────┘
                                    │
                                    ▼
                              output.mp4 + poster.jpg
```

Смысл разбиения: **шаг 2 можно заменить целиком** — с шаблонного
генератора на LLM — не трогая шаги 1 и 3, потому что контракт между ними
фиксирован.

### 1.3 Путь от видео до MP4 (пошагово)

1. **Приём.** `POST /api/videos/create` (multipart) — файл пишется на диск
   потоком, без буферизации в память. Длительность проверяется `ffprobe`
   **до** списания кредита (потолок `MAX_DURATION_SEC`, по умолчанию 180 с):
   клиент не платит за файл, который мы не возьмём. Списание кредита, запись
   `videos` и задача `jobs` идут одной транзакцией: при сбое любого шага
   кредит не пропадает. Повтор запроса отсекается ключом идемпотентности
   (`X-Request-Id` → уникальный индекс `videos(user_id, client_token)`)
   и окном в 15 секунд для клиентов без ключа.
2. **Подхват.** Воркер раз в 3 секунды забирает задачу через `SKIP LOCKED`.
3. **Разбор дорожки.** Провайдер речи (`openai` / `groq` / свой) отдаёт
   слова с таймингами. Без ключа — запасной путь: `ffprobe` даёт
   длительность, `ffmpeg silencedetect` — интервалы тишины, инверсия
   тишины = участки речи, длинные дробятся на куски ~0.9 с.
4. **Разметка.** Шаблон раскладывает акценты (каждая N-я реплика), врезки
   (каждые M секунд), титульную плашку из названия.
5. **Рендер.** Пропсы пишутся во временный JSON-файл (длинный JSON не влезает
   в аргументы командной строки), запускается `npx remotion render`.
   Прогресс парсится из stdout по регулярке `(\d{1,3})%`.
6. **Обложка.** `ffmpeg -ss 1.2` вырезает кадр для плитки.
7. **Доставка.** Статус `ready`, ролику выдаётся `share_token`, уведомление
   в бот со ссылкой `/dl/<токен>`. Файлом ролик не отправляется: Bot API
   не пропускает больше 50 МБ. Ссылка поддерживает Range-запросы.
8. **Уборка.** Исходник удаляется через `SOURCE_KEEP_DAYS` (окно правок),
   готовый ролик — через `OUTPUT_KEEP_DAYS`, после чего статус становится
   `expired`, а токен обнуляется.

**Замеренная производительность** (Apple M2, 8 ядер, ролик 50.4 с):

| Этап | Время |
|---|---|
| Разбор дорожки по паузам | 1.7 с |
| Разметка | 0.005 с |
| Рендер, полный кадр | 136 с |
| Рендер, черновик 45% | 82 с |

Дорого обходятся `filter: blur()` на склейках и второй видеослой: тот же
ролик с более плотной ручной разметкой (26 склеек против 22, 6 врезок
против 4) на загруженной машине рендерился 539 с.

Каждый ролик пишет свои метрики в `videos`: `speech_provider`, `speech_ms`,
`speech_words`, `plan_ms`, `render_ms`, `source_bytes`, `output_bytes`.
Без них себестоимость считать не из чего — логи контейнера теряются
при каждом редеплое.

---

## 2. Remotion: структура компонентов

### 2.1 Точка входа

```
src/index.jsx     registerRoot(RemotionRoot)
src/Root.jsx      <Composition id="Full" component={Reel}
                    width=1080 height=1920 fps=30
                    durationInFrames={50.4 * 30} />
```

Формат жёстко вертикальный 1080×1920, 30 fps.

### 2.2 Дерево компонентов и зона ответственности

```
Reel.jsx  ← композитор, собирает все слои и считает камеру
│
├── Fonts.jsx ......... @font-face через staticFile (5 семейств)
│
├── OffthreadVideo .... ФОН: исходное видео, на нём transform+filter
│                       (камера, рывок склейки, расфокус)
│
├── Sequence + OffthreadVideo ... ВСТАВКИ: живой B-roll поверх фона
│   └── BrollCard.jsx ........... ВСТАВКИ: графическая карточка,
│                                 если файла нет
│
├── AbsoluteFill (rgba) ......... ПЕРЕХОДЫ: цветная вспышка на стыке
│
├── Sequence + Audio ............ ЗВУК: призвуки по меткам
│
└── один из трёх, взаимоисключающе:
    ├── TitleCard.jsx ... ТЕКСТ: титульная плашка (первые 3.5 с)
    ├── Shout.jsx ....... ТЕКСТ: слово-выкрик крупно в кавычках
    └── Subtitles.jsx ... ТЕКСТ: пословные субтитры
```

### 2.3 Кто за что отвечает

| Задача | Файл | Как реализовано |
|---|---|---|
| **Текст** | `Subtitles.jsx`, `TitleCard.jsx`, `Shout.jsx` | Обычный DOM + CSS. Градиентная заливка через `background-clip: text` |
| **Анимации** | все компоненты | `spring()` для входа, `interpolate()` для линейных переходов |
| **Переходы** | `Reel.jsx` → `useCutEffect` | Рывок масштаба + вспышка + расфокус на метках `cuts.json` |
| **Эффекты** | `Reel.jsx` | `filter: blur()`, `drop-shadow`, цветные оверлеи |
| **Камера** | `Reel.jsx` → `useCameraZoom` | `transform: scale()` на видеослое |
| **Вставки** | `broll.js` + `BrollCard.jsx` | Видеофайл в `Sequence` либо генерируемая карточка |
| **Фон** | `Reel.jsx` | `OffthreadVideo` с `objectFit: cover` |
| **Изображения** | **не реализовано** | См. раздел 7 |

### 2.4 Почему `OffthreadVideo`, а не `Video`

`OffthreadVideo` извлекает кадр через ffmpeg вместо HTML5-видео. Это
детерминированно (при рендере кадр 450 — всегда один и тот же) и не зависит
от готовности видеоэлемента. Обязателен для многослойного видео.

### 2.5 Почему `Sequence` вокруг B-roll

Без `Sequence` внутренний `OffthreadVideo` отсчитывает время от начала
композиции. Врезка на 38-й секунде показала бы 38-ю секунду короткого клипа,
то есть чёрный кадр. `Sequence from={…}` сдвигает локальную систему
координат, а `startFrom` выбирает точку внутри самого клипа.

---

## 3. Система субтитров

### 3.1 Два регистра текста

Основа визуального ритма — перепад между проходными и смысловыми словами.

| Регистр | Кегль | Начертание | Цвет | Когда |
|---|---|---|---|---|
| `SUB.base` | 62 px | Montserrat 700 | белый | по умолчанию |
| `SUB.accent` | 104 px | Montserrat 800, КАПС | золотой градиент | слово попало в интервал акцента |

### 3.2 Как текст разбивается на строки

Whisper режет речь по своей логике: строка висит, когда человек уже говорит
дальше, и обрывается посреди мысли. Поэтому реплики **пересобираются на лету**
в `retime.js` — исходный `chunks.json` не меняется.

Алгоритм — жадный выбор шва с оценочной функцией `seamScore`:

```js
score  = +120   если слово заканчивает предложение  [.!?…]
         +45    если заканчивает клаузу             [,;:—–-]
         +25    если это последнее слово исходного чанка
         +85    если следующее слово с заглавной (новая мысль)
         +min(35, пауза_до_следующего * 120)
         −90    если слово служебное (предлог, союз, местоимение)
         −|длина − 3| * 7  −  превышение * 11
```

Затем два прохода склейки огрызков:
- **назад** — короткая строка приклеивается к предыдущей, но никогда через
  конец предложения и никогда если начинается с заглавной;
- **вперёд** — начало новой мысли, которое нельзя приклеить назад,
  приклеивается к следующей строке.

Результат на реальном материале: 57 исходных чанков → 60 строк со средней
длительностью 0.78 с, ноль строк короче 0.3 с, ноль обрывов на предлоге.

### 3.3 Хранение таймкодов

Абсолютные секунды от начала исходника, на двух уровнях:

```jsonc
{
  "start": 0.28,        // начало реплики
  "end":   1.34,        // конец реплики
  "words": [
    {"text": "Как",      "start": 0.28, "end": 0.28},
    {"text": "кредитка", "start": 0.28, "end": 0.82}
  ]
}
```

Перевод в кадры — только в момент рендера:
`enterFrame = Math.round((word.start − fromSeconds) * fps)`.

**Важная деталь.** `word.end` от Whisper растянут до начала следующего слова.
Для расчёта конца строки используется оценка реальной длительности:

```js
realEnd = min(word.end, word.start + max(0.26, число_букв * 0.085))
```

Без этого строка висит лишние 0.5–0.9 секунды после того, как слово отзвучало.

### 3.4 Как определяется ключевое слово

Сейчас — **позиционно, по таблице интервалов** в `src/accents.js`:

```js
export const ACCENTS = [
  [2.88, 2.90, 'нищим'],
  [6.42, 6.72, 'год без процентов'],
  // …
];
export const isAccent = (time) =>
  ACCENTS.some(([from, to]) => time >= from && time < to);
```

Слово получает акцентный регистр, если **его `start` попадает в интервал**.
Третий элемент — комментарий для человека, в коде не используется.

Это ровно та точка, куда встраивается LLM: она должна вернуть такой же
массив интервалов, и больше ничего в системе не изменится.

### 3.5 Автоподгонка размера

Длинные слова («БЕСПРОЦЕНТНЫХ») при 104 px не влезают в кадр. Ширина
оценивается без замеров в DOM — по таблице средних ширин символов:

```js
CHAR_W = {caps: 0.74, lower: 0.62, other: 0.36}   // в долях кегля
SIDE_MARGINS = 0.64                                // отступы вокруг слова
```

Самое длинное слово реплики задаёт масштаб для **всех** слов этого регистра,
иначе соседние слова прыгали бы в размере. Ниже `SUB.minFit = 0.6` не ужимает.

### 3.6 Эффекты на уровне слова

| Эффект | Реализация | Параметры |
|---|---|---|
| **scale** (появление) | `interpolate(appear, [0,1], [0.72, 1])` | от 72% |
| **bounce** (акцент) | `spring` c низким damping | `damping: 11, stiffness: 240, mass: 0.4` |
| **pop** (активное слово) | `scale × (1 + 0.08 × pop)` | +8% в момент произнесения |
| **opacity** | `= appear` (значение пружины) | 0 → 1 |
| **translateY** | `interpolate(appear, [0,1], [22, 0])` | въезд снизу на 22 px |
| **fade-out строки** | `interpolate` по времени | 0.14 с, укорачивается для коротких строк |
| **highlight** | смена регистра `base` → `accent` | размер + капс + градиент |
| **rotation** | только в `Shout` | −3° |
| **blur** | только на видеослое | см. раздел 5 |

Ключевой приём: **непрозвучавшее слово занимает своё место, но невидимо**
(`opacity: 0`, но в потоке). Иначе строка прыгала бы при появлении каждого
следующего слова.

### 3.7 Позиционирование и слепые зоны

```js
SAFE = {
  top: 0.065,     // шапка площадки
  bottom: 0.2,    // подпись, музыка, кнопки — у TikTok почти 400 px
  side: 0.045,    // края, которые площадка обрежет
  icons: {from: 0.565, width: 0.185},  // колонка лайк/коммент/шер
  centerX: 0.43,  // центр видимой части, а не кадра
  face: 0.42,     // нижняя граница лица автора
}
```

Два неочевидных решения:

1. **Строка растёт вниз от фиксированной верхней кромки** (`SUB.topY = 0.5`),
   а не центрируется. При центрировании появление второй строки поднимает
   блок вверх и он накрывает подбородок.
2. **Горизонтальный сдвиг на `(centerX − 0.5) × width`** — текст
   центрируется по видимой области без колонки кнопок, а не по кадру.

---

## 4. Форматы данных

### 4.1 `chunks` — расшифровка (вход пайплайна)

```jsonc
[
  {
    "start": 0.28,
    "end": 1.34,
    "words": [
      {"text": "Как", "start": 0.28, "end": 0.28}
    ]
  }
]
```

Это ровно то, что отдаёт Whisper с `word_timestamps=True`.

### 4.2 `cuts` — метки склеек

```jsonc
[
  {"t": 3.4, "kind": "base"},    // обычная склейка внутри речи
  {"t": 6.3, "kind": "broll"},   // вход в перебивку
  {"t": 7.9, "kind": "back"}     // возврат на говорящего
]
```

`kind` выбирает силу рывка, цвет вспышки и глубину расфокуса.

### 4.3 `ACCENTS` — что подсветить

```jsonc
[[6.42, 6.72, "год без процентов"]]
//  from   to    подпись для человека
```

### 4.4 `SHOUTS` — слово-выкрик

```jsonc
[{"from": 47.64, "to": 48.66, "text": "ЧАТ"}]
```

### 4.5 `BROLL` — вставки

Два взаимоисключающих вида:

```jsonc
// живое видео
{"from": 6.11, "to": 8.38, "file": "shopping.mp4", "startFrom": 0.5}

// генерируемая карточка
{"from": 21.6, "to": 24.1, "card": {
  "type": "stat",                    // stat | rows | tags
  "label": "КОГДА ПЕРИОД КОНЧИЛСЯ",
  "count": [0, 80], "suffix": "%",   // счётчик вместо статичного value
  "caption": "годовых на весь долг",
  "alarm": true                      // красный градиент вместо золотого
}}
```

### 4.6 `CUES` — звуковые метки (вычисляются, не хранятся)

```jsonc
[{"at": 6.11, "name": "whoosh-hi", "volume": 0.7}]
```

### 4.7 `plan` — единый монтажный план

Формат, который возвращает `server/pipeline/plan.js`. **Это целевая структура
для генерации моделью:**

```jsonc
{
  "version": 1,
  "provider": "template",           // template | model
  "template": "expose",
  "palette": "wine",

  "title": {
    "in": 0.15, "out": 3.46,
    "lines": [
      {"kind": "big",   "text": "Как кредитка"},
      {"kind": "badge", "text": "без процентов"}
    ]
  },

  "chunks": [ /* см. 4.1 */ ],
  "accents": [[6.42, 6.72, "год без процентов"]],
  "broll":   [{"from": 6.1, "to": 8.4, "file": null}],
  "shouts":  [{"from": 47.6, "to": 48.7, "text": "ЧАТ"}]
}
```

### 4.8 Рекомендуемая структура для LLM

Запрошенный вами формат «одна запись на событие» удобнее для генерации —
модели проще выдать плоский список, чем несколько параллельных массивов:

```jsonc
{
  "events": [
    {
      "time": 6.42,
      "duration": 0.3,
      "type": "accent",              // accent | shout | broll | title | cut
      "text": "год без процентов",
      "style": "gold-caps",          // ключ пресета из стиль-кита
      "animation": "pop",            // ключ пресета анимации
      "importance": 0.9              // 0..1 — насколько сильно выделять
    }
  ]
}
```

Адаптер из этого формата в четыре внутренних массива пишется за 30 строк.
`importance` можно замапить на силу эффекта: кегль акцента, глубину
camera punch, громкость призвука.

---

## 5. Анимационные пресеты

Все анимации построены на двух примитивах Remotion:
`spring({frame, fps, config})` и `interpolate(value, inputRange, outputRange)`.

### 5.1 Появление текста — `word-appear`

| | |
|---|---|
| **Назначение** | Слово въезжает снизу с лёгким перелётом |
| **Где** | `Subtitles.jsx` |
| **Тип** | spring |
| **Параметры** | `damping: 13, stiffness: 190, mass: 0.55` |
| **Применяется к** | `scale: 0.72→1`, `translateY: 22→0`, `opacity: 0→1` |

### 5.2 Удар по активному слову — `word-pop`

| | |
|---|---|
| **Назначение** | Слово подрастает ровно в момент произнесения |
| **Тип** | spring, более резкая |
| **Параметры** | `damping: 11, stiffness: 240, mass: 0.4` |
| **Эффект** | множитель `1 + 0.08 × pop` к масштабу |

### 5.3 Исчезновение строки — `line-fade`

| | |
|---|---|
| **Назначение** | Строка гаснет, а не пропадает рывком |
| **Тип** | interpolate по времени |
| **Параметры** | `SUB.fadeOut = 0.14 с`, но не больше половины длительности строки |

### 5.4 Вход строки плашки — `title-line`

| | |
|---|---|
| **Где** | `TitleCard.jsx` |
| **Тип** | spring |
| **Параметры** | `damping: 14, stiffness: 170, mass: 0.6` |
| **Эффект** | `translateY: 40→0`, `scale: 0.86→1`, `opacity: 0→1` |
| **Каскад** | `TITLE.stagger = 0.08 с` между строками |

### 5.5 Уход плашки — `title-leave`

| | |
|---|---|
| **Тип** | interpolate по времени |
| **Параметры** | `TITLE.fadeOut = 0.25 с` |
| **Эффект** | `scale: 1 → 1.06`, `opacity: 1 → 0` (уход «на зрителя») |

### 5.6 Появление выкрика — `shout-in`

| | |
|---|---|
| **Где** | `Shout.jsx` |
| **Тип** | spring с низким damping (заметный перелёт) |
| **Параметры** | `damping: 9, stiffness: 210, mass: 0.7` |
| **Эффект** | `scale: 0.55→1`, постоянный наклон −3° |

### 5.7 Дыхание выкрика — `shout-breathe`

| | |
|---|---|
| **Назначение** | Слово живёт на экране, а не висит статично |
| **Тип** | синусоида |
| **Формула** | `1 + 0.03 × sin(local/fps × 3.4) × min(1, appear)` |

### 5.8 Появление элемента карточки — `card-rise`

| | |
|---|---|
| **Где** | `BrollCard.jsx` |
| **Тип** | spring |
| **Параметры** | `damping: 15, stiffness: 180, mass: 0.6` |
| **Эффект** | `translateY: 34→0`, `opacity: 0→1` |
| **Каскад** | `CARD.stagger = 0.06 с` |

### 5.9 Штамп цифры — `stat-stamp`

| | |
|---|---|
| **Назначение** | Крупная цифра прилетает, а не проявляется |
| **Тип** | spring |
| **Параметры** | `damping: 11, stiffness: 200, mass: 0.8` |
| **Эффект** | `scale: 1.7 → 1` |

### 5.10 Счётчик — `stat-count`

| | |
|---|---|
| **Назначение** | Рост показателя видно, а не читаешь |
| **Тип** | interpolate + `Easing.out(Easing.cubic)` |
| **Параметры** | `countFrames: 26` (около 0.9 с) |
| **Данные** | `count: [0, 80]`, `suffix: "%"` |

### 5.11 Блик по цифре — `stat-glare`

| | |
|---|---|
| **Тип** | interpolate по прогрессу врезки |
| **Параметры** | старт `0.42` от длины врезки, длительность `0.3` |
| **Реализация** | второй слой текста с градиентом `transparent → белый → transparent`, позиция градиента едет от −120% до 220% |

### 5.12 Наезд камеры (общий) — `camera-drift`

| | |
|---|---|
| **Назначение** | Кадр медленно оживает на всей длине |
| **Тип** | линейный interpolate по номеру кадра |
| **Параметры** | `CAMERA.drift = 0.05` — плюс 5% масштаба от начала к концу |

### 5.13 Рывок камеры на акценте — `camera-punch`

| | |
|---|---|
| **Назначение** | Подчёркивает смысловое слово |
| **Тип** | spring, затухание |
| **Параметры** | `damping: 18, stiffness: 90, mass: 0.9`, амплитуда `0.035`, окно `0.75 с` |
| **Формула** | `scale += 0.035 × (1 − settle)` |

### 5.14 Рывок на склейке — `cut-punch`

| | |
|---|---|
| **Тип** | interpolate + `Easing.out(Easing.cubic)` |
| **Параметры** | амплитуда `0.028`, `16 кадров` |

### 5.15 Вспышка — `cut-flash`

| | |
|---|---|
| **Тип** | interpolate + `Easing.out(Easing.quad)` |
| **Параметры** | `8 кадров` |
| **Цвет и сила** | `base: белый 0.10` · `broll: красный 0.18` · `back: белый 0.12` |

### 5.16 Расфокус — `cut-blur`

| | |
|---|---|
| **Назначение** | Взгляд цепляется за наводку резкости сильнее, чем за рывок |
| **Тип** | interpolate + `Easing.out(Easing.cubic)` |
| **Параметры** | `9 px` обычная склейка, `20 px` вход в перебивку, `11 кадров` |

### 5.17 Вход и выход врезки — `broll-fade`

| | |
|---|---|
| **Тип** | двойной interpolate (min из двух) |
| **Параметры** | `fadeIn: 0.16 с`, `fadeOut: 0.24 с`, `blurIn: 18 px` |

**Пресета `shake` в системе нет.** Реализуется добавлением
`translateX/Y` с синусоидой и затуханием по тому же принципу, что
`camera-punch`.

---

## 6. Механики удержания внимания

Замеры на реальном 50-секундном ролике.

| Механика | Частота | Как реализована |
|---|---|---|
| **Смена субтитра** | каждые **0.78 с** | `retime.js`: `maxWords: 3`, `maxDur: 1.1 с` |
| **Склейка** | 26 меток, каждые **~2 с** | `cuts.json` |
| **Врезка** | 6 штук, каждые **~7 с**, длина 2.3–3.1 с | `broll.js` |
| **Акцент** | 14 штук, каждые **~3.5 с** | `accents.js` |
| **Наезд камеры** | на каждом акценте | `useCameraZoom` |
| **Призвук** | 27 меток | `sfx.js`, выводятся из событий |

### 6.1 Когда происходит zoom

Два независимых слоя:
- **постоянный дрейф** +5% на всю длину — кадр никогда не статичен;
- **рывок +3.5%** в момент начала акцента, затухает за 0.75 с.

Важно: рывок привязан к **акцентному слову**, а не к таймеру. Внимание
подкрепляется в момент смысловой нагрузки, а не механически.

### 6.2 Когда меняется текст

Строка живёт от 0.32 с (минимум, чтобы не мигать) до 1.1 с (максимум).
Реальное среднее 0.78 с. Строка **гаснет вместе с последним словом**,
а не висит до следующей реплики.

### 6.3 Как выделяются важные моменты

Смысловое слово получает **пять усилений одновременно**:

1. кегль ×1.68 (62 → 104 px);
2. КАПС;
3. золотой градиент вместо белого;
4. рывок камеры +3.5%;
5. призвук-щелчок.

Совпадение всех пяти в одной точке — то, что читается как «важное».

### 6.4 Правила, выведенные опытным путём

| Правило | Почему |
|---|---|
| Один момент — один звук | Два призвука подряд слышны как ошибка |
| Соседние события — разной высоты | 14 одинаковых щелчков превращаются в стук |
| Минимальный зазор между звуками 0.22 с | Иначе каша |
| Расфокус вместо рывка на склейке | Наводка резкости притягивает взгляд сильнее |
| Врезка не короче времени чтения | Формула: `0.55 с` заголовок + `0.75 с` строка + `0.38 с` тег |

---

## 7. Визуальные вставки

### 7.1 Что реализовано

| Тип | Статус | Как |
|---|---|---|
| **B-roll (видео)** | ✅ реализовано | `Sequence` + `OffthreadVideo`, `objectFit: cover`, вход/выход через fade + blur |
| **Карточки** | ✅ реализовано | `BrollCard.jsx`, три типа: `stat` / `rows` / `tags` |
| **Картинки** | ❌ нет | |
| **GIF** | ❌ нет | |
| **Мемы** | ❌ нет | |
| **Иконки** | ❌ нет | |
| **Эмодзи** | ❌ нет | |

### 7.2 Типы карточек

```jsonc
// stat — крупная цифра со счётчиком, штампом и бликом
{"type": "stat", "label": "…", "count": [0, 80], "suffix": "%",
 "caption": "…", "alarm": true}

// rows — «выписка»: пары ключ-значение с разделителями
{"type": "rows", "label": "…", "rows": [["Задолженность", "145 000 ₽"]]}

// tags — набор плашек-чипсов
{"type": "tags", "label": "…", "tags": ["банкротство", "кредиты"]}
```

### 7.3 Как добавить картинки, GIF и эмодзи

Изображения — самое простое расширение, Remotion даёт готовые примитивы:

```jsx
import {Img, staticFile} from 'remotion';
<Img src={staticFile('memes/shock.png')} />
```

Для **GIF** нужен отдельный пакет `@remotion/gif` — обычный `<img>`
с анимированным GIF при рендере отдаст только первый кадр, потому что
Remotion рендерит покадрово, а не в реальном времени.

**Эмодзи** — просто текст в компоненте, но нужен шрифт с эмодзи
(Noto Color Emoji), иначе headless-браузер отрисует пустые прямоугольники.

Точка встраивания одна — блок выбора врезки в `Reel.jsx`:

```jsx
{broll?.file  ? <видео />
 : broll?.card ? <BrollCard />
 : broll?.image ? <Img />        // ← добавляется сюда
 : null}
```

---

## 8. Конфигурация

### 8.1 Где что лежит

| Файл | Что настраивает | Правится без изменения логики |
|---|---|---|
| `src/style.js` | **весь визуал**: палитра, кегли, шрифты, тайминги анимаций, слепые зоны | ✅ полностью |
| `src/accents.js` | что подсветить, где выкрик | ✅ данные |
| `src/broll.js` | врезки и их содержимое | ✅ данные |
| `src/cuts.json` | метки склеек | ✅ данные |
| `src/sfx.js` | громкости и правила прореживания звука | ✅ параметры |
| `server/config.js` | окружение, лимиты, ключи | ✅ через `.env` |

### 8.2 Блоки стиль-кита

```js
PALETTE   // 7 цветов
SAFE      // слепые зоны площадок + граница лица
SUB       // субтитры: два регистра, посадка, автоподгонка
TIMING    // пересборка реплик: длина, склейка, минимальный показ
SHOUT     // слово-выкрик
CARD      // графические карточки: 5 подблоков
TITLE     // титульная плашка
CAMERA    // дрейф и рывок
CUT       // рывок, вспышка, расфокус на склейках
```

Каждый параметр снабжён комментарием с обоснованием — почему именно это
значение.

### 8.3 Что меняется через `.env` (без кода)

`RENDER_CONCURRENCY`, `RENDER_TIMEOUT_MIN`, `PREVIEW_COST`, `PREVIEW_SCALE`,
`MAX_UPLOAD_MB`, `PACKAGE_DAYS`, `REFERRAL_BONUS`, ключи касс и модели.

Плюс три группы, добавленные при стабилизации MVP:

| Группа | Переменные | Что даёт |
|---|---|---|
| Провайдер речи | `SPEECH_PROVIDER`, `SPEECH_API_KEY`, `SPEECH_URL`, `SPEECH_MODEL`, `SPEECH_MAX_AUDIO_MB`, `SPEECH_WORD_TIMESTAMPS` | смена сервиса распознавания без правки кода |
| Сроки хранения | `SOURCE_KEEP_DAYS`, `OUTPUT_KEEP_DAYS`, `STORAGE_SWEEP_MIN` | потолок расходов на диск |
| Защита от дублей | `CREATE_COOLDOWN_SEC` | окно отсечки двойного нажатия |

Пресеты провайдеров лежат в `SPEECH_PRESETS` (`server/config.js`): адрес,
модель и лимит размера дорожки. Точечные переменные перебивают пресет —
так подключается сервис, которого нет в списке.

---

## 9. Структура файлов

```
reels-studio/
│
├── src/                          ★ ЯДРО РЕНДЕРА (переносится целиком)
│   ├── index.jsx                 registerRoot — точка входа Remotion
│   ├── Root.jsx                  <Composition>: 1080×1920, 30 fps, длительность
│   ├── Reel.jsx                  композитор: слои, камера, склейки, звук
│   │
│   ├── Subtitles.jsx             пословные субтитры, два регистра, автоподгонка
│   ├── TitleCard.jsx             титульная плашка со строками и бейджами
│   ├── Shout.jsx                 слово-выкрик крупно в кавычках
│   ├── BrollCard.jsx             графические карточки stat / rows / tags
│   ├── fonts.jsx                 @font-face для пяти семейств
│   │
│   ├── style.js                  ★ СТИЛЬ-КИТ: весь визуал одним файлом
│   ├── retime.js                 ★ пересборка реплик по смысловым швам
│   │
│   ├── accents.js                разметка акцентов и выкриков
│   ├── broll.js                  разметка врезок
│   ├── sfx.js                    вывод звуковых меток из событий
│   │
│   ├── chunks.json               расшифровка с пословными таймингами
│   └── cuts.json                 метки склеек
│
├── server/                       бэкенд (специфичен для этого продукта)
│   ├── index.js                  точка входа: миграции, бот, API, воркер
│   ├── config.js                 чтение окружения, падение при нехватке
│   ├── db.js                     пул Postgres + автомиграции
│   ├── auth.js                   проверка подписи Telegram Mini App (HMAC)
│   ├── users.js                  пользователи, кредиты, реферальная программа
│   ├── packages.js               тарифы
│   ├── lava.js                   касса: счета и вебхуки
│   ├── api.js                    HTTP: маршруты, загрузка, отдача медиа
│   ├── bot.js                    команды бота, уведомления
│   ├── worker.js                 ★ очередь: подхват, повторы, возврат кредита
│   │
│   └── pipeline/                 ★ ТРИ ШАГА ОБРАБОТКИ
│       ├── transcribe.js         ffmpeg → границы реплик
│       ├── plan.js               шаблоны → монтажный план
│       └── render.js             запуск Remotion CLI, парсинг прогресса
│
├── tools/
│   ├── vad.mjs                   огибающая громкости, детект речи
│   └── make-sfx.mjs              ★ генератор звуков (синтез WAV без сэмплов)
│
├── miniapp/index.html            интерфейс Telegram Mini App
├── migrations/001_init.sql       схема базы
├── public/
│   ├── fonts/                    5 TTF (Montserrat, Oswald, …)
│   ├── sfx/                      7 сгенерированных WAV
│   ├── broll/                    видео-врезки 9:16
│   └── base.mp4                  исходник для студии
│
├── package.json                  скрипты: start, dev, worker, studio, render
├── railway.json / nixpacks.toml  деплой (ffmpeg + chromium в образе)
└── .env.example                  все переменные с комментариями
```

★ — то, что имеет смысл переносить.

---

## 10. Перенос в ShortsAI

Ваш пайплайн: **Whisper → анализ текста → монтажный план → рендер.**
Первые два шага у вас уже есть и лучше моих. Переносить нужно последний.

### 10.1 Что переносится как есть

| Модуль | Строк | Зачем |
|---|---|---|
| `src/style.js` | 245 | Весь визуал. Меняете значения — получаете свой стиль |
| `src/Reel.jsx` | 210 | Композитор слоёв, камера, склейки |
| `src/Subtitles.jsx` | 163 | Субтитры с двумя регистрами и автоподгонкой |
| `src/TitleCard.jsx` | 110 | Титульная плашка |
| `src/Shout.jsx` | 60 | Слово-выкрик |
| `src/BrollCard.jsx` | 290 | Карточки (если нужны) |
| `src/retime.js` | 165 | **Пересборка реплик — забирайте обязательно** |
| `src/fonts.jsx` | 25 | Подключение шрифтов |
| `tools/make-sfx.mjs` | 120 | Генератор звуков без сторонних сэмплов |

**`retime.js` — самое ценное здесь.** Он не зависит ни от чего, кроме
объекта `TIMING`, и решает проблему, которая есть у любого Whisper-пайплайна:
строки висят не в такт речи и обрываются на предлогах.

### 10.2 Что переносить не нужно

`server/` целиком (бот, касса, кредиты), `miniapp/`, `migrations/` — это
специфика продукта, а не монтажа.

### 10.3 Зависимости

```json
{
  "dependencies": {
    "remotion": "^4.0.506",
    "@remotion/cli": "^4.0.506",
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  }
}
```

Плюс в системе: **ffmpeg** и **Chromium** (Remotion рендерит через headless).
На Railway/Docker ставится через `nixpacks.toml` — файл можно скопировать.

Опционально: `@remotion/gif`, если нужны анимированные вставки.

### 10.4 ⚠️ Главная работа при переносе

**В текущей реализации есть архитектурный разрыв, который вам придётся
закрыть.**

`server/pipeline/plan.js` генерирует объект `plan` и передаёт его в пропсы.
Но `Reel.jsx` его **не читает** — он импортирует разметку напрямую из
статических модулей:

```js
import {ACCENT_STARTS, shoutAt} from './accents';   // ← хардкод
import {brollAt} from './broll';                    // ← хардкод
import cuts from './cuts.json';                     // ← хардкод
```

То есть визуальная часть отлажена на одном конкретном ролике, а генерация
плана написана, но ещё не подключена. Для ShortsAI, где план приходит от
модели на каждое видео, **это обязательно переделывать**.

Переделка небольшая — заменить импорты на чтение из пропсов:

```jsx
// было
import {isAccent} from './accents';

// станет
export const Reel = ({chunks, plan, fromSeconds = 0}) => {
  const isAccent = (t) => plan.accents.some(([a, b]) => t >= a && t < b);
  const brollAt  = (t) => plan.broll.find((b) => t >= b.from && t < b.to) ?? null;
  const shoutAt  = (t) => plan.shouts.find((s) => t >= s.from && t < s.to) ?? null;
  const cuts     = plan.cuts;
  // …
```

Затронуты: `Reel.jsx`, `Subtitles.jsx` (импорт `isAccent` и `brollAt`),
`sfx.js` (строит метки из `BROLL` и `ACCENTS`). Оценка: **полдня работы.**

### 10.5 Какие модули создать у себя

**1. Адаптер Whisper → chunks** (~40 строк)

```js
export const toChunks = (whisperSegments) =>
  whisperSegments.map((seg) => ({
    start: seg.start,
    end: seg.end,
    words: seg.words.map((w) => ({
      text: w.word.trim(),
      start: w.start,
      end: w.end,
    })),
  }));
```

**2. Адаптер LLM → plan** (~60 строк)

Промпт должен возвращать плоский список событий (раздел 4.8), адаптер
раскладывает его в четыре массива: `accents`, `broll`, `shouts`, `cuts`.
Обязательна валидация: время в пределах длительности, интервалы не
пересекаются, `to > from`.

**3. Вызов рендера** (~90 строк) — можно взять `pipeline/render.js` целиком.
Ключевые детали, на которых легко споткнуться:

- пропсы передавать **файлом** (`--props=file.json`), не аргументом —
  длинный JSON не влезает в лимит командной строки;
- прогресс парсить из stdout по `(\d{1,3})%`;
- ставить таймаут с `SIGKILL` — зависший Chromium не умирает сам.

### 10.6 Как связать AI-анализ с Remotion

Контракт между анализом и рендером — один JSON. Промпт для модели должен
описывать не «сделай красиво», а конкретные поля:

```
На входе транскрипт с таймингами. Верни JSON:
{
  "accents": [[start, end, "слово"]],   // 1 акцент на 3–4 секунды
  "broll":   [{"from": …, "to": …, "query": "поисковый запрос"}],
  "shouts":  [{"from": …, "to": …, "text": "СЛОВО"}],
  "title":   {"lines": [{"kind": "big|badge", "text": "…"}]}
}

Правила:
— акцент ставь на смысловое слово: цифру, вывод, противопоставление;
— интервал акцента = интервал самого слова из транскрипта, не шире;
— врезка 2.5–3 с, не чаще чем раз в 6 секунд;
— выкрик только один, в конце, на призыве к действию.
```

Числовые ориентиры для промпта берите из раздела 6 — они выведены
из работающего материала, а не из общих соображений.

### 10.7 Порядок работ

| Шаг | Что | Оценка |
|---|---|---|
| 1 | Скопировать `src/`, поставить зависимости, отрендерить тестовый ролик на своих данных | 2 ч |
| 2 | Развязать `Reel.jsx` от статических импортов (раздел 10.4) | 4 ч |
| 3 | Написать адаптер Whisper → chunks, проверить `retime.js` на своём языке | 2 ч |
| 4 | Написать промпт и адаптер LLM → plan, добавить валидацию | 1 день |
| 5 | Настроить `style.js` под свою айдентику | 2–4 ч |
| 6 | Прикрутить очередь и прогресс | 4 ч |

### 10.8 Грабли, на которые уже наступили

| Проблема | Решение |
|---|---|
| Строка висит после того, как слово отзвучало | `realEnd` вместо `word.end` (раздел 3.3) |
| Строка обрывается на предлоге | `seamScore` со штрафом −90 (раздел 3.2) |
| Слова слипаются в одно | Межсловный интервал не меньше `0.32em` |
| Длинное слово режется о край кадра | `fitScale` с оценкой ширины (раздел 3.5) |
| Текст перекрывают кнопки площадки | Блок `SAFE`, особенно `icons.from` |
| Текст наезжает на лицо | Растить строку вниз от фиксированной кромки |
| Короткая строка мигает 0.1 с | `minShow` + два прохода склейки |
| B-roll показывает чёрный кадр | `Sequence` вокруг вложенного видео |
| Рендер зависает навсегда | Таймаут с `SIGKILL` |
| Два призвука сливаются | Прореживание по `minGap` |
