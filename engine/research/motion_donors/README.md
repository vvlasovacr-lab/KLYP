# ShortsAI Motion Donor Research Sandbox

This directory is isolated from the production renderer.

- External repositories live only under `sources/`.
- Adapted experiments live under `sandbox/` and `candidates/`.
- Production code must never import directly from `sources/`.
- `TALKING_HEAD_V1_RC` remains the rollback baseline.

