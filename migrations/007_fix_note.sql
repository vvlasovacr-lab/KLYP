-- Правка ко всему ролику, а не к моменту.
--
-- Раньше замечание можно было оставить только на отрезке таймлайна:
-- отметил секунду, написал что не так. Для «убери эту врезку» это
-- удобно, а для «сделай текст во всём ролике мельче» — невозможно:
-- такая просьба не привязана ни к какой секунде.
--
-- fix_note — просьба ко всей сборке целиком. Метки на таймлайне
-- остались и работают вместе с ней.
ALTER TABLE videos ADD COLUMN IF NOT EXISTS fix_note TEXT;
