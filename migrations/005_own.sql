-- Свои материалы клиента.
--
-- Он приносит собственные врезки и музыку: у эксперта есть съёмка с
-- объекта, скриншоты переписки, фирменный трек. Наша библиотека их не
-- заменит, потому что она общая, а это его материал.
--
-- clips — список файлов в том порядке, в каком клиент их выбрал: они
-- встанут в те моменты, которые режиссёр отвёл под врезки.
-- music — один файл, подмешивается под голосом на всю длину ролика.
ALTER TABLE videos ADD COLUMN IF NOT EXISTS clips JSONB;
ALTER TABLE videos ADD COLUMN IF NOT EXISTS music TEXT;
